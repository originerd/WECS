<%= form_for(@box) do |f| %>
  <% if @box.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@box.errors.count, "error") %> prohibited this calendar from being saved:</h2>

      <ul>
      <% @box.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class: "field">
    <%= f.label :name, "이름" %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class: "field">
    <%= f.label :description, "정보" %><br>
    <%= f.text_field :description, class: "form-control" %>
  </div>

  <div class: "actions">
    <%= f.submit "저장", class: "btn btn-lg btn-primary btn-block" %>
  </div>
<% end %>
